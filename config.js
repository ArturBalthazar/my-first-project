const config = {
    scene: {
        camera: {
            alpha: Math.PI / 2,
            beta: Math.PI / 4,
            radius: 10,
        },
        light: {
            intensity: 0.7,
        },
    },
    loadingScreen: {
        message: "Loading assets, please wait...",
    },
};

export default config;
